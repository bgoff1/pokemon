import { EncountersComponent } from './encounters.component';

describe('EncountersComponent', () => {
  let component: EncountersComponent;

  beforeEach(() => {
    component = new EncountersComponent();
  });

  test('should create', () => {
    expect(component).toBeTruthy();
  });
});
