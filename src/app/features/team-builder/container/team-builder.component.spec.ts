import { TeamBuilderComponent } from './team-builder.component';

describe('Team Builder Component', () => {
  test('should create', () => {
    expect(new TeamBuilderComponent()).toBeTruthy();
  });
});
